# Static provider components example

This example demonstrates one required component and two optional components,
including a simple dependency. The `.fixture` payloads are harmless test data,
not redistributed mods. Replace their URLs, sizes, and hashes with artifacts
you are legally allowed to distribute before using the manifest in production.
